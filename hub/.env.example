# Server only. Copy to hub/.env; never commit the populated file.
OPENAI_API_KEY=
OPENAI_LIVE_VOICE=marin
OPENAI_LIVE_TIMEOUT=30
# Optional override; keep task rules in the backend rather than the voice prompt.
# OPENAI_LIVE_INSTRUCTIONS=
