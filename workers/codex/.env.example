# Server Configuration
HOST=0.0.0.0
PORT=8004
DEBUG=false

# Codex CLI Configuration
CODEX_PATH=codex
CODEX_MODEL=
CODEX_PROFILE=
CODEX_DEFAULT_TIMEOUT=30
CODEX_MAX_TIMEOUT=300

# Security
ALLOWED_ORIGINS=*

# Note: Authentication is managed by the hub system
# Workers are only accessible via hub (not exposed externally)
